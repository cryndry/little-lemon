import React from "react";
import s from "./ConfirmedBooking.module.css";

const ConfirmedBooking = () => {
    return (
        <div className={s.ConfirmedBooking}>ConfirmedBooking</div>
    );
};

export default ConfirmedBooking;